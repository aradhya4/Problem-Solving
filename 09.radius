#include <stdio.h>
#include <math.h>
    int main() {
    float r,area;
    printf("enter the radius of the circle: ");
    scanf("%f",&r);

    area= 2*r*r;
    printf("The area of largest possible rectangle(square) is %f",area);
}

